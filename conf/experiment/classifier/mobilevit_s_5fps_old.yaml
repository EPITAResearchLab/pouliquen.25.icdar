# @package _global_

model:
  model: 
    backbone_name: mobilevit_xxs

paths:
  frame_skip: 3 # for 5fps

training:
  trainer:
    epochs_max: 15

task_name: classifierfinal_old_5fps_s${seed}_${model.model.backbone_name}_${paths.split_name}
