# @package _global_
defaults:
  - override /model: ml_model
  - override /valtransform: torchtransformssub_pil

paths:
  data_cropped_dir: ${paths.data_dir}/cropped_sub/
  frame_skip: 3 # for 5fps

training:
  datamodule:
    notransformations: True
  # trainer:
  #   accelerator: cpu
    # epochs_max: 1

task_name: holodetectorfffffinal_notransform_5fps_s${seed}_hog-sgd_${paths.split_name}