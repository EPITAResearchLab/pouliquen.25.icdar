# @package _global_

model:
  model: 
    backbone_name: mobilevit_xxs

paths:
  data_cropped_dir: ${paths.data_dir}/cropped_sub/
  frame_skip: 3 # for 5fps

training:
  datamodule:
    notransformations: True
  # trainer:
  #   accelerator: cpu
    # epochs_max: 1

task_name: holodetectorfinal_notransfo_5fps_s${seed}_${model.model.backbone_name}_${paths.split_name}
